/*
1) for statment
2) for sections
    starting point of the loop
        initialization statement
    loop condition
        as long as loop condition is true, loop will continue
    update statement
        and of every iteration it will update
3) loop body

for(i=1(initialization/onlyonce); i <= num(loopconditionischeckedoneveryiterationandexecutedafterinitialization); i++(updatestatement,executedlastofeveryiteration);)
    {
        printf("%d", i); //loopbodyexecutedaftercondition,beforeupdatestaement
    }

*/